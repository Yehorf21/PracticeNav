import { StyleSheet } from 'react-native';

export const artStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  art: {
    fontSize: 16,
    textAlign: 'center',
    lineHeight: 28,
    fontFamily: 'Courier',
    color: '#444',
  },
});
